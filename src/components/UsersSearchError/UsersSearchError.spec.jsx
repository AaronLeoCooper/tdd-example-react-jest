import React from 'react';
import { shallow } from 'enzyme';

import UsersSearchError from './UsersSearchError';

const render = (props = {}) => shallow(
  <UsersSearchError {...props} />
);

describe('UsersSearchError', () => {
  
});
